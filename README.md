# is_flappy
Check a list of hosts to see if a service is listening on a given port.

## Usage
To run via CLI:
```
cli
├── is_flappy
└── README.md
```

To run as a service:
```
service
├── Dockerfile
├── is_flappy.py
├── README.md
└── requirements.txt
```
